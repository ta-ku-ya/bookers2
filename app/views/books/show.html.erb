<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
<%= render 'user info', books: @books %>
<div class=" contaoner">
 <div class="row">
　<div class="col-lg-8 offset-lg-1">
　 <h2>Book detail</h2>
  <table>
   <thead>
    <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    </tr>
   </thead>

   <tbody>
    <tr>
     <td><%= @user.name %>
      <%= image_tag @user.get_profile_image(100,100) %>
     </td>
     <td><%= @book.title %></td>
     <td><%= @book.body %></td>
     <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
     <td><% if @book.user == current_user %>
      <%= link_to "Destroy", book_path(@book.id), method: :delete %></td>
         <% end %>
    </tr>
   </tbody>
</table>
</div>
</div>
</div>
</div>
</div>
</div>